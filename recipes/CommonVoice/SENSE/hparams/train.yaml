# ########################################################################################
# Model: Multilingual SENSE on Common Voice
# Loss: cosine similarity (1 - dot) * loss_scale
# Authors: Mariem Bouziane 2025, Salima Mdhaffar 2025, Haroun Elleuch 2025, Yannick Estève 2025
# ########################################################################################

seed: 2422
__set_seed: !!python/object/apply:torch.manual_seed [!ref <seed>]
debug: False

output_folder: !ref results/SENSE_training/<seed>
save_folder: !ref <output_folder>/save
train_log: !ref <output_folder>/train_log.txt

################################################################################
# Data
################################################################################

# Root Common Voice folder (contains en/, fr/, br/, sv-SE/, etc. with train/dev/test.tsv + clips/)
data_folder: !PLACEHOLDER #eg:/mypath/a-recette-speechbrain/data

# Multilingual sampling parameter (alpha in the language ratio formula)
sampling_alpha: 0.05

# Files produced by prep.py (prepare_sense)
language_ratios_file: !ref <output_folder>/language_ratios.json
train_csv: !ref <output_folder>/train.csv
valid_csv: !ref <output_folder>/dev.csv

# Data preparation control
skip_prep: False      # Set True if CSV and JSON files are already prepared
convert_to_wav: True  # Recommended for w2v-bert (mono wav 16 kHz)
sample_rate: 16000    # Target sampling rate

################################################################################
# Text encoder: BGE (on-the-fly embeddings)
################################################################################
# Requires: pip install FlagEmbedding
bge_path: "BAAI/bge-m3"
bge_use_fp16: True   # Set False to use full precision (fp32)

bge_model: !new:FlagEmbedding.BGEM3FlagModel [!ref <bge_path>, !ref <bge_use_fp16>]

################################################################################
# Speech encoder: wav2vec / w2v-bert (Hugging Face)
################################################################################
wav2vec2_hub: facebook/w2v-bert-2.0
wav2vec2_frozen: False

################################################################################
# Training
################################################################################
number_of_epochs: 100
lr: 1.5
lr_wav2vec: 0.00001
sorting: random
batch_size: 2
test_batch_size: 2
ckpt_interval_minutes: 45

epoch_counter: !new:speechbrain.utils.epoch_loop.EpochCounter
    limit: !ref <number_of_epochs>

dataloader_options:
    batch_size: !ref <batch_size>
    num_workers: 8

test_dataloader_options:
    batch_size: !ref <test_batch_size>
    num_workers: 8

################################################################################
# Architecture
################################################################################
d_model: 1024
loss_scale: 50

# W2VBert integration (see speechbrain.integrations.huggingface.w2v_bert)
wav2vec2: !new:speechbrain.integrations.huggingface.w2v_bert.W2VBert
    source: !ref <wav2vec2_hub>
    save_path: !ref <save_folder>/wav2vec2_checkpoint
    freeze: !ref <wav2vec2_frozen>

attn_pooling: !new:speechbrain.nnet.pooling.AttentionPooling
    input_dim: !ref <d_model>

lin: !new:torch.nn.Linear
    in_features: !ref <d_model>
    out_features: !ref <d_model>

modules:
    wav2vec2: !ref <wav2vec2>
    attn_pooling: !ref <attn_pooling>
    lin: !ref <lin>

model: !new:torch.nn.ModuleList
    modules: [!ref <attn_pooling>, !ref <lin>]

################################################################################
# Optimizers & schedulers
################################################################################
adam_opt_class: !name:torch.optim.Adadelta
    lr: !ref <lr>
    rho: 0.95
    eps: 0.00000001

wav2vec_opt_class: !name:torch.optim.Adam
    lr: !ref <lr_wav2vec>

lr_annealing_adam: !new:speechbrain.nnet.schedulers.NewBobScheduler
    initial_value: !ref <lr>
    improvement_threshold: 0.0025
    annealing_factor: 0.5
    patient: 2

lr_annealing_wav2vec: !new:speechbrain.nnet.schedulers.NewBobScheduler
    initial_value: !ref <lr_wav2vec>
    improvement_threshold: 0.0025
    annealing_factor: 0.9

################################################################################
# Checkpointer & logger
################################################################################
checkpointer: !new:speechbrain.utils.checkpoints.Checkpointer
    checkpoints_dir: !ref <save_folder>
    recoverables:
        model: !ref <model>
        wav2vec2: !ref <wav2vec2>
        lr_annealing_adam: !ref <lr_annealing_adam>
        lr_annealing_wav2vec: !ref <lr_annealing_wav2vec>
        counter: !ref <epoch_counter>

train_logger: !new:speechbrain.utils.train_logger.FileTrainLogger
    save_file: !ref <train_log>
