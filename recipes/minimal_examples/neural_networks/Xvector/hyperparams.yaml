# Basic parameters
seed: 1234
__set_seed: !apply:torch.manual_seed [!ref <seed>]

# Data (csv) files
data_folder: !PLACEHOLDER
csv_train: !ref <data_folder>/train.csv
csv_valid: !ref <data_folder>/dev.csv
csv_test: !ref <data_folder>/test.csv

# Training hyperparameters
number_of_epochs: 10
batch_size: 6
lr: 0.002
device: 'cpu'

# Feature parameters
n_mels: 24
left_frames: 0
right_frames: 0
deltas: False

# Number of speakers
out_n_neurons: 2

train_data: !new:speechbrain.data_io.legacy.ExtendedCSVDataset
    csvpath: !ref <csv_train>
    output_keys: [wav, spk_id]
    sorting: ascending
    replacements:
        data_folder: !ref <data_folder>

valid_data: !new:speechbrain.data_io.legacy.ExtendedCSVDataset
    csvpath: !ref <csv_valid>
    output_keys: [wav, spk_id]
    sorting: ascending
    replacements:
        data_folder: !ref <data_folder>

label_encoder: !new:speechbrain.data_io.encoder.CategoricalEncoder

loader_kwargs:
    batch_size: !ref <batch_size>
    collate_fn: !name:speechbrain.data_io.batch.PaddedBatch
        padded_keys: ["wav"]


compute_features: !new:speechbrain.lobes.features.Fbank
    n_mels: !ref <n_mels>
    left_frames: !ref <left_frames>
    right_frames: !ref <right_frames>
    deltas: !ref <deltas>

xvector_model: !new:speechbrain.lobes.models.Xvector.Xvector
    in_channels: !ref <n_mels>
    activation: !name:torch.nn.LeakyReLU
    tdnn_blocks: 5
    tdnn_channels: [512, 512, 512, 512, 1500]
    tdnn_kernel_sizes: [5, 3, 3, 1, 1]
    tdnn_dilations: [1, 2, 3, 1, 1]
    lin_neurons: 512

classifier: !new:speechbrain.lobes.models.Xvector.Classifier
    input_shape: [null, null, 512]
    activation: !name:torch.nn.LeakyReLU
    lin_blocks: 1
    lin_neurons: 512
    out_neurons: !ref <out_n_neurons>

mean_var_norm: !new:speechbrain.processing.features.InputNormalization
    norm_type: global

modules:
    xvector_model: !ref <xvector_model>
    classifier: !ref <classifier>
    mean_var_norm: !ref <mean_var_norm>

opt_class: !name:torch.optim.Adam
    lr: !ref <lr>

compute_cost: !name:speechbrain.nnet.losses.nll_loss

error_stats: !name:speechbrain.utils.metric_stats.MetricStats
    metric: !name:speechbrain.nnet.losses.classification_error
        reduction: batch
