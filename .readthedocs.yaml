# .readthedocs.yaml

version: 2

build:
  os: ubuntu-20.04
  image: latest
  tools:
    python: "3.9"

python:
  install:
    - requirements: docs/docs-requirements.txt

# Don't build any extra formats
formats: []
